enum StatusEnumType {
  AVAILABLE = 'available',
  ONARIDE = 'onaride',
}

enum RoleEnumType {
  RIDER = 'rider',
  DRIVER = 'driver',
}

enum PaymentMethodTypeEnum {
  CARD = 'CARD',
  NEQUI = 'NEQUI',
}

export { StatusEnumType, RoleEnumType, PaymentMethodTypeEnum }
